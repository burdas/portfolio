---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const experience = await getCollection('experience');
  return experience.map((job) => ({
    params: { slug: job.slug },
    props: { job }
  }));
}

const { job } = Astro.props;
const { data: jobData } = job;
---

<Layout title={`${jobData.role} - ${jobData.company}`} description={jobData.description}>
  <article class="min-h-screen pt-24 pb-16">
    <div class="max-w-4xl mx-auto px-6">
      <a href="/#experience" class="inline-flex items-center gap-2 text-gray-500 hover:text-white transition mb-8">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
        Volver a Experiencia
      </a>
      
      <header class="mb-12">
        <span class="text-sm text-gray-500">{jobData.period}</span>
        <h1 class="text-4xl md:text-5xl font-bold mt-2 mb-4">{jobData.role}</h1>
        <p class="text-xl text-gray-400">{jobData.company}</p>
      </header>

      <div class="prose prose-invert prose-lg max-w-none mb-12">
        <p class="text-gray-300 leading-relaxed">{jobData.description}</p>
      </div>

      <section>
        <h2 class="text-2xl font-semibold mb-6">Highlights</h2>
        <ul class="space-y-4">
          {jobData.highlights.map((highlight) => (
            <li class="flex items-start gap-3 text-gray-400">
              <span class="text-white mt-1">→</span>
              {highlight}
            </li>
          ))}
        </ul>
      </section>
    </div>
  </article>
</Layout>
